<header id="intro">
    <div id="into_username" class="p-2">
        <div class="h3 m-0">{{ site.title }}</div>
        <div>@stupied.net</div>    
    </div>
    <div id="intro_img"></div>
    <div id="into_content" class="p-2">
        <h1 class="mb-0">{{ page.title }}</h1>
        <time datetime="" class="h5">
            {{ site.time }}
        </time>
        <p>Okay was thinking it be neat to try bootstrap out, but after seeing the 120kb (this is after removing majority of the features) I realize that it really isn't that worth it. Let this commit be a cautionary tale to my felow indie webdevers,,,</p>
    </div>
</header>